# This container configuration is populated based on `directory_type` in ldap.yaml, but can be overridden if you want.
# Not specifying a `directory_type` requires you to populate this configuration manually.
#
# Please see https://github.com/rbw0/lapdance/wiki/User-and-Group-containers for more info

