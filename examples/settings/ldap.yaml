uri: ldaps://ad.company.com:636
base_dn: ou=stuff,dc=ad,dc=company,dc=com
conn_timeout: 5  # Give up after this many seconds

# Authentication
bind_dn: cn=LDAP Admin,ou=stuff,dc=ad,dc=company,dc=com
secret: bind_password

# Return details about LDAP errors to the client
return_error_details: true
