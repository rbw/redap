# Here you may define or--if using a non-custom type--override, any parts of the schema
#
# Check out the wiki for more info:
# https://github.com/rbw0/redap/wiki/Working-with-schemas

{
  relative_dn: 'cn=users,cn=accounts'
}
